node_modules/
dist/
dist-bundle/
*.tsbuildinfo
*.depo
depose-output/
.turbo/

# Pack artifacts
*.tgz

# Go binaries
depose-shim
depose-verify-*
apps/verify/build/
# `go build ./...` inside apps/capture-shim names its output after the
# directory rather than the Makefile's BINARY, so it lands here uninvited.
apps/capture-shim/capture-shim

# OS
.DS_Store
*.swp
*.swo

# Keys (never commit)
.depose/keys/

# IDE
.vscode/
.idea/

# Test artifacts
coverage/

# Local dev scratch
*.bak
_fix_*.py
debug-*.ts

# Internal planning / marketing, not part of the shipped tool.
# These live in the repo for working copies but should never ship.
update-plan.md
docs/update-plan-2.md
docs/launch/
